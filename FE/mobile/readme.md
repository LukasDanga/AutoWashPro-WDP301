React Native screen: `LoginRegisterScreen.js`

How to use:

1. Copy `LoginRegisterScreen.js` into your React Native project (e.g., `screens/`).
2. Import: `import LoginRegisterScreen from './screens/LoginRegisterScreen';` then register in navigation or render directly.
3. Replace `Alert` calls with real API requests to your backend.

Notes:
- This file uses basic React Native primitives and inline styles. You can import `styles.js` for a shared style object.
